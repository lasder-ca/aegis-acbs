# Internationalization

The Web UI and CLI labels support Japanese, English, Simplified Chinese, Korean and French.

Translation catalog keys live in `internal/i18n/i18n.go`. English is the fallback. The test suite checks that every supported catalog resolves every English key.

The graph algorithm is language-neutral. OSM road names are not needed for benchmark correctness, so the compact graph format stores topology, coordinates and weights only.
